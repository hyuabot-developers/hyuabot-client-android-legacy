query ReadingRoom($roomName : String!, $campusId : Int!, $isActive : Boolean!) {
    readingRoom(roomName: $roomName, campusId: $campusId, isActive: $isActive) {
        roomName,
        activeSeat,
        availableSeat,
    }
}